# Acceptance review: c92a918

Date: 13 September 2026. Reviewed commit: c92a91881a017d3d3d4a619dce77b840013a82cc, draft PR #1 at inspection. Includes R3 changes since the prior eb8b4c8 review and the corpus pilot.

**Decision: partially accept the corpus-integrity improvements and retained failed-experiment record. The ChemDraw project, P0-C, native visual quality, gold corpus and M2 generalization are not accepted. One new P2 verifier defect remains.**

## P2: independently bind and validate arrowhead calibration

[verify_composition.py:143](https://github.com/saigyujikingyo-png/chemdraw-companion/blob/c92a91881a017d3d3d4a619dce77b840013a82cc/probes/verify_composition.py#L143) directly adds the scene's conservative_forward_extension_pt to its target-port tolerance, without independently checking finite range or calibration binding.

An independent synthetic CDXML/scene baseline passes. Moving the target endpoint 30 pt, while synchronizing the mapping to preserve the coordinate-consistency control, correctly fails the nitrogen_donation target anchor. Changing only the scene allowance to 1,000,000 incorrectly restores semantic_and_native_inventory to pass. This is a subcheck false positive; no ChemDraw process was run and no complete visible-head mask bypass is claimed.

[mechanism_composer.py:183](https://github.com/saigyujikingyo-png/chemdraw-companion/blob/c92a91881a017d3d3d4a619dce77b840013a82cc/runtime/mechanism_composer.py#L183) checks bond length, font size and HeadSize, but accepts the retained 0.6 pt stroke calibration when the requested stroke is 1.2 pt. Bond-length/font-size mismatch controls correctly reject. Use a shared calibration reader that binds the measurement hash, applicable native build, stroke and complete head parameters. The verifier must independently check finite, nonnegative, bounded values; the scene must not expand its own acceptance tolerance. Preserve the separate actual visible-head mask gate.

## Independently verified

- 51 runtime/probe tests and 36 corpus tests pass on Python 3.12.14. Initial environment failures are retained: missing numpy in default Python, then incompatible Python-specific dependencies. A dedicated review dependency directory resolved them without global changes.
- All 282 current public source-archive entries match size/SHA-256; all 70 current source files match staged Git hashes.
- All 241 retained R3 archive entries match, including 24 native-output entries; all 58 source hashes match their actual 4ca3ffd commit. These are retained records, not fresh c92a918 native runs.
- Current verifier replay on retained R2 native files gives S1 pass, R1 pass and M1 fail. M1 preserves the visible-tip/contact failures and additionally reports 120 unclassified mixed-colour pixels. Five prior real-CDXML style corruption controls are rejected. This is retained-image replay, not current-rule native regeneration. Scene equivalence alone does not infer missing captions on both sides; low-residual colour mixtures remain a disclosed raster ambiguity, not a demonstrated complete-gate bypass.
- Six corpus fixes and their negative tests are present: split revision, IR/sample identity, mechanism asset kind, ancestry cycle, duplicate correction asset references, annotation dependency cycle. Eight extra independent controls behave as expected.
- Reviewed runtime changes have no Beckmann/M1/M2/candidate-ID layout branch. Candidate constants remain in the explicit fixture builder. Bond-sink mask relationships are explicitly unsupported.

The fixture builder defines a runtime candidate and then programmatically encodes that same candidate into corpus form. This is fixture-specific runtime-to-corpus encoding, not an independent chemical oracle or production corpus-to-runtime lowering. Complete native correction-diff generation remains unimplemented.

## Gate decisions

| Gate | Conclusion |
| --- | --- |
| P0-A transport | Earlier bounded creation/save/reopen/export evidence remains. Current pilot workers are reported, but new raw files were not received by this reviewer; no new independent pass. |
| P0-B object control | Historical limited subsets remain. Current nonzero CDX/CDXML edit/save/reopen claims await independent raw-file checking; full object CRUD is not qualified. |
| P0-C composition | Failed: reported nitrogen visible-head failure, one glyph-box failure, four ink gaps and unresolved ink remain after two repairs. |
| Owner visual review | Rejected for LP orientation, arrow pointing and exaggerated bends. This is neither timed correction nor chemistry/physical-size approval. |
| M1/M2 | Historical M1 failure remains. Changed rules have not requalified S1/R1/M1/M2; scored base M2 was not run. |
| Freeze/holdout | Not started/not selected. Schema examples and saved-geometry permutations do not prove anti-selection or unseen-input performance. |
| Corpus/gold | One ungraded exposed-and-tuned development candidate is reported. Gold = 0; human active correction time is null. Production projection and full correction engine remain absent. |

The private owner archive is declared as 1,954,700 bytes, SHA-256 f0265ff53be8709fb098743c9de8534b44b57e2183df0b9447c1a6eacc9c57e0, with 507 entries and 38 native originals. Those counts were not independently checked here: the public source archive deliberately excludes private native/corpus assets. A remote inventory request returned no usable final receipt and the existing SSH route timed out. No connection configuration or sharing permissions changed. The reported 48-asset original bundle and 50-asset rejection registry remain producer-verified claims pending actual receipt.

## Next gate

Keep this pilot, its first output and two repairs immutable. Fix the calibration guard with independent synthetic controls; do not silently reopen the exhausted experiment or fit on M1/M2 and derivatives. Transfer the existing private package through an authorized owner channel, verify its bytes, then replay its edit/readback/mask and corpus checks. Future gold claims require actual chemical/visual review and measured human correction evidence where claimed.

IR/Composer and native render-measure feedback remain the immediate R&D priority. Evidence neither proves complete native object control nor justifies building a replacement editor. MCP, installer, extra hosts and release remain deferred. Base M2 must pass before an immutable freeze and independently selected untuned asymmetric-oxime holdout.

See [machine-readable receipt](c92a918-checks.json). This review changes documentation only; it does not merge implementation, alter native artifacts or grant release approval.
