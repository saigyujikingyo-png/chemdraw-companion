# Quality Oracle + Gold Correction Corpus: active handoff

Effective 13 September 2026. This is the active successor to the Composer R&D / Dataset Phase. Read DEVELOPMENT_PRINCIPLES.md, CHEMBRIDGE.md, CLOUD_STORAGE.md, QUALITY_ORACLE_V1.md, GOLD_CORRECTION_CORPUS_V1.md and GOLD_SILVER_HOLDOUT_V1.md.

## Current state

The user accepted c92a918 as an R&D delivery, not as an accepted ChemDraw product. Its reviewed runtime and corpus tests passed (51 and 36 respectively), but native visual quality failed, M2 was not scored and gold remains zero. Historical M1 and the separate pilot have visual failures; the owner's LP orientation, arrow pointing and excessive-bend rejection must be retained on the exact figure it reviewed. Neither refers to a newly approved output.

Two trust-boundary aspects of the existing P2 remain in that implementation: calibration is not bound to the full native style, and an independent readback subcheck can trust an enlarged scene allowance. This phase adds a separately tested calibration/oracle prototype; it does not silently patch or qualify the existing runtime.

The latest private pilot archive declares 38 native originals. Source summaries and checksums are not substitutes for actually receiving and hashing those bytes. Record exact receipt success or the current retrieval blocker separately. Do not publish the private archive, change sharing/connection configuration or create alternate native output to fill this gap.

## Work order

1. Establish trustworthy reference custody, native-render provenance, complete object inventory and measurable visual signals. Keep chemical correctness, native editability, visual quality and human acceptance separate.
2. Evaluate eight independent metric families and adversarial controls. Use final native ink, separately supplied reference geometry and a versioned policy. Never expand all tolerances, move the expected target toward a bad arrow or add measured head extension to the port acceptance threshold.
3. Validate the gold correction schema and immutable first/final evidence. Record full object correspondence, typed deltas, source/rights, actual active human correction time and separate real reviews. Synthetic controls and AI annotations confer no gold status.
4. Only after the evidence workflow is usable, establish a small high-value set: pilot five, coverage ten, working target thirty within a 20-50 range. This is a selection strategy, not samples already acquired. The current task does not collect, scrape or train.
5. On the implementation branch, a later scoped integration must adopt the calibration guards and native exporter/extractor contracts, then demonstrate current native results. No prototype test substitutes for that integration or for human acceptance.

## M1/M2 policy change

The latest explicit user instruction permits **M1 for development**, with all prior exposure, failures and corrections preserved. Older M1 lock wording is superseded for new work; historical manifests and review receipts must not be rewritten. M1 must never be relabelled unseen merely because it was renamed or redrawn.

**M2 Beckmann Snake, all its descendants and future unseen holdout remain frozen against fitting and training.** Do not derive rules, prompts, priors, critic/ranker parameters or correction training data from their answers or failures. No Beckmann template, reference-specific absolute coordinates, state-number branches or screenshot patches are permitted. Reference 7/6/15 counts remain sample specifications, not Composer constants.

Base M2 must pass before implementation/rules/style/adapter/cache freeze and independent evaluator selection of an untuned asymmetric-oxime holdout. No holdout is selected here. Known-family unseen input and family-disjoint novelty remain different claims. Permission to develop M1 does not reopen the separate pilot's exhausted two-repair experiment.

## Compatibility and boundaries

The new quality/correction profiles live under contracts/quality and do not replace mechanism-ir/0.1 or the runtime composer request. Accepted c92 corpus-integrity fixes are synchronized into the architecture contracts. Existing corpus examples retain their historical hashes and split declarations; new quality/gold records use the current policy.

Keep MCP, installer, host expansion, multi-agent product packaging, replacement-editor work and new feature surfaces paused. Internal bounded implementation/review work does not introduce a product orchestration layer. Do not train a learned model or build an end-to-end CDXML predictor.

Do not resume the archived computer-manager relay, the OpenAI frontend issue or cross-task coordination with Origin. Keep work and evidence within the existing ChemDraw tasks and authorized private/local destinations.

## Required next receipt

Report exact code/schema/policy/reference hashes; actual per-metric pass/fail/unmeasured outcomes; negative controls; native provenance and received-file checks; all remaining extraction/occlusion/coverage limits; true human review and timing; sample tier/split/lineage; and gold counts. Distinguish structural eligibility from authenticated human evidence, prototype checks from integrated Composer behavior, and received assets from producer claims.
