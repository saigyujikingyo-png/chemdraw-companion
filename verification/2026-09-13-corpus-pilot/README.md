# Independent corpus pilot: reviewed contracts, ungraded native evidence

This checkpoint merges architecture commit `052cca91bc149956c7ea81ac33aafd1c8f87d6ee` and reviews its five corpus contracts. It is a Composer R&D / Dataset Phase checkpoint, **not native quality acceptance, a gold corpus release or a model-training run**.

## Contract review

The architecture baseline passed its 30 existing tests. Six independently injected integrity errors also passed that baseline: stale split revision; IR/sample identity disagreement with otherwise valid asset hashes; wrong mechanism asset kind; cyclic asset ancestry; duplicate correction asset references; cyclic annotation dependencies. The bounded validator now rejects all six, also checks split policy agreement and proper IR/annotation MIME/kind, and reports actual validated gold counts. The original and repaired control reports and source hashes are retained. The five schema definitions and runtime request contract are unchanged. **36 corpus tests and 51 runtime/probe tests pass.** Existing M1/M2 contract controls remain regression-only.

## One independently authored development candidate

`development-alkoxide-ammonium-01` declares ethoxide + ammonium -> ethanol + ammonia: two states, one transition and two simultaneous two-electron flows. The transferred H is explicit; the atom inventory, charge balance, bond changes and LP slots are supplied. It is an AI-proposed teaching-level bookkeeping example without solvent, kinetics or experimental mechanism evidence. It shares the broad proton-transfer family with a schema probe; separate authorship is not family novelty.

The first request constructor used incorrect runtime-envelope fields and failed before any native call. Its source is retained; correcting the envelope did not alter chemistry. The fixture builder separately encodes this same intended chemistry into the current runtime and archival corpus profiles. **There is no production corpus-to-runtime lowering.** No benchmark, annotation, native page coordinates or answer oracle is imported by this fixture authoring path.

The development candidate was then used in a declared two-repair experiment. Its bundle is `ungraded`, `development`, `exposed_and_tuned`. Current `uses` flags forbid further fit, training, evaluation and corpus redistribution; these prospective flags do not erase the recorded tuning history. The first output and both repairs remain immutable. This experiment is closed.

## Native result and implementation changes

| Attempt | Chemical/control readback | Visible native result |
| --- | --- | --- |
| First | Failed N-recovery target control port | Failure retained; no original-attempt mask pass claimed |
| Repair 1 | Both formats pass implemented chemical/control/inventory subset | N visible head and several ink gaps fail |
| Repair 2 | Both formats pass same subset; zero warnings | N visible head fails, one glyph-box check fails, four ink gaps fail, one unclassified ink pixel remains |

General development changes use measured asymmetric label bounds for radial atom ports, the independent R3 geometric head-extension calibration, generic bond/glyph clearance checks and bounded lone-pair radius selection. The adapter retains observed native label alignment and emits the measured native arrow width/center fields. The control-endpoint allowance is separate from the mandatory actual visible-head mask test. These changes **did not achieve native visual acceptance**; native glyph alignment is still context-sensitive in the tested output. No further repair is inferred from near-threshold gaps.

The mask sidecar now binds LPs by component, atom and pair slot. Its small generic relationship adapter supports atom sinks and rejects unqualified bond sinks explicitly. This is a test-only adapter. All generations use the existing runtime IR/Composer/native CDXML path; no replacement editor, MCP, installer, new host, training or scraping is added.

Native execution retains six completed workers with 60-second bounds, owned HWND/PID binding, and 38 original native files: intrinsic geometry, three page/edit runs and two colour-only mask runs. All journalled file hashes match. Five preexisting ChemDraw processes remain untouched. COM-reported implicit-H/style representations are not substituted for native XML chemistry or text-run checks.

The pilot's saved intrinsic geometry also passes catalog/flow/bond reorder, bijective relabel and 37-degree rotation comparisons; the existing clearance failure remains. This is deterministic development replay, not fresh native variants or unseen evaluation. The original metamorphic report's static "Historical" scope label is retained with an explicit context sidecar; it does not denote M1/M2 input.

## Local corpus and human-review evidence

The local sample bundle binds **48 assets**, **39 proposed observations across all eight layers**, and **two actual automatic edit records**, with five-schema validation and cross-file hash/reference checks. CDX and CDXML each have a declared nonzero atom, LP, curve-control and title edit that survives independent save/reopen. All captured unrelated scene properties are compared. Native atom/H/charge/bond graphs remain equal. Complete visible electron-flow port semantic equivalence remains `unverified`; this is not a production correction-diff engine.

The issued packet initially had zero human reviews. The owner subsequently **rejected the visual result**: wrong lone-pair orientation, insufficiently accurate arrow pointing, and excessively large/exaggerated bends in some arrows. This actual user feedback is retained in a separate review registry, with the original packet and unreviewed bundle unchanged. The review registry checks 50 assets and adds three document-level observations (42 total). Individual objects, viewing scale and a reviewer-supplied timestamp were not provided; no localization, physical-size approval or chemical sign-off is invented. The machine-recorded receipt time is separate.

There is now **one owner visual rejection, zero gold samples and no measured human active correction time**. The correction records say `edit_probe`, actor `tool`, never human correction. Title edits are presentation-only scene entities, separate from chemical occurrences. The three-page review PDF embeds unchanged original native PNGs at a measured 170 x 110 mm canvas size. It is an assembled review document, not native PDF export or physical-size human acceptance. Independent chemical review, a satisfactory future visual result, and actual timed human before/after evidence remain absent. The two-repair budget is exhausted; the rejection does not silently authorize another native repair run.

The recorded pilot rights grant local storage/annotation only; no training or corpus-redistribution grant is inferred. **Full raw native and corpus assets are delivered through the local owner artifact channel, not this public directory.** Public content here contains source snapshots, checksums and sanitized engineering summaries. No vendor binaries, credentials or private academic data are included. The local archive digest and full native-file manifest support a later explicitly authorized evidence handoff.

## Locked regression and next gate

M1/M2 and all descendants remain exposed fixed regression evidence, excluded from further fitting/training. This pilot does not rerun or requalify S1/R1/M1/M2 under the changed rules. Historical R3 status remains S1/R1 automatic mask pass with owner review pending; M1 preflight/visual failure; new scored base M2 not run. R2/R3 archives are unchanged.

Base M2 must pass before code/rules/style/adapter/cache freeze and independent evaluator holdout selection. No holdout has been selected. A known-family unseen input cannot support family-disjoint novelty. Corpus/gold review is the remaining human dependency; completing local evidence does not confer acceptance.

The enclosing commit and staged-source audit bind this checkpoint. `source-evidence.zip` contains actual first/repair/final source snapshots and integrity-control reports; it contains no private pilot corpus assets. The local owner delivery adds all raw execution evidence and the sample bundle. Earlier source versions are not replaced by later verifier or packaging code.
